# Prompt

Create a diagram with a relationship to an undefined alias.

## Acceptance Criteria
- Must be detected as a negative test (fails validation)
- Fails because the 'db' alias used in Rel is never defined
